<UserControl x:Class="LogTailer.Ui.Pages.File.FileDisplayView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <syncfusion:SfDataGrid  x:Name="LinesDataGrid"
                                   ItemsSource="{Binding TextLines, UpdateSourceTrigger=PropertyChanged}"
                                   ColumnSizer="AutoWithLastColumnFill"
                                   FilterRowPosition="FixedTop"
                                   SelectedItem="{Binding SelectedLine, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   SelectionChanged="SelectionChanged">
            <syncfusion:SfDataGrid.Columns>
                <syncfusion:GridDateTimeColumn MappingName="Timestamp" Pattern="FullDateTime"/>
                <syncfusion:GridTextColumn MappingName="Text"/>
            </syncfusion:SfDataGrid.Columns>
            </syncfusion:SfDataGrid>
    </Grid>
</UserControl>
